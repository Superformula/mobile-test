import 'package:flutter/material.dart';

class TapToExpandFAB extends StatelessWidget {
  const TapToExpandFAB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
