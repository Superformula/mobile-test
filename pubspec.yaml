name: qrapp
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: 0.17.0
  provider: 6.0.1
  dio: 4.0.1
  qr_code_scanner: 0.6.1
  qr_flutter: 4.0.0
  lottie: 1.2.1
  simple_speed_dial: 0.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
   - assets/lottie/16589-qrcode-scanner.json
